# AI 指挥系统重构计划

截至 2026-09-20。

本文件只写尚未实现的路线：当前下一步、门槛、架构约定、阶段验收。唯一目标见 `AGENTS.md`。已实现证据见 `AI_IMPLEMENTED_FEATURES_ACCEPTANCE.md`。当前构建的逆向见 `AI_REVERSE_ENGINEERING.md`。仓库里有启动器、`bridge.dll` 和占位 `planner.exe`，不表示下列阶段已经完成。

## 当前下一步

唯一目标要求两件事一起成立：指挥已有部队，并用原生命令控制战区、集团军群、集团军、战线。

编制约束：战区和集团军群都必须至少挂一个集团军。不能先建空战区/空集团军群再往里填。第一条集团军会带出它需要的父级（`--army` 已对照：集团军 + 集团军群 + 「德国第1战区」）。再开一个战区或集团军群，是把**另一个**集团军调过去；来源里必须还剩至少一个集团军。手建第二个集团军后，底部「新战区」就是这条路。

当前做：钉 `CAssignToTheaterGroupCommand`（「新战区」），对已有两个集团军里的一个发，源战区留下另一个。不要对唯一的集团军发 `--ag` 或「新战区」，那会掏空现有容器。`CSetTheatreCommand` 仍是指派已有战区，不发。单独 `--ag` 未对照，且不是下一步。德国 `--cancel`、`GetPlayer`、读档不挡这一条。一种订单成功不能推断另一种可用。

## 门槛

单师闭环做不到就放弃项目：

```text
实时读取位置和组织度
-> 原生移动被游戏接受、地图显示、师实际执行
-> 观察窗口内订单保持，或该国陆军 AI 已被抑制
-> 读档后旧对象失效，条件变化后可取消或重规划
```

观察窗口：连续 12 个游戏小时，或该师到达目标省份，以先到者为准。窗口内原版或 BlackICE 不得改写该师订单，除非该国陆军 AI 已被抑制。

```text
注入、读师、原生移动做不到 -> 放弃
陆军行动者压不住 -> 放弃
GetPlayer、读档未过 -> 闭环未成立，不得进入 P1 之后，不得标已实现
P0 过 -> P1–P3，并可为后续订单做原生命令取证
M0 过 -> P4
订单各自取证 -> 对应的 P5 / P6
```

瑞士手控移动只证明能读、能发，不能当抗覆盖。单师移动不能当防线或战役控制的证据。战区/集团军的原生命令取证不必等 P6；P6 是用已经取证的订单做战役规划。

## 原生订单

按顺序取证。优先走玩家操作经过的命令路径。每种必须单独通过：创建 → 游戏接受 → 地图显示 → 实际执行。

1. 单师移动（德国 `51/1` 已对照）
2. 单师取消（瑞士已对照；德国未测，不挡后面）
3. 集团军（`--army` 已对照；同一次也长出集团军群和第一个战区，因为父级不能空）
4. 额外战区 ← 当前：至少两个集团军，再 `CAssignToTheaterGroupCommand` 把其中一个调到新战区
5. 额外集团军群：至少两个集团军，再 `CArmyGroupCommand` 把其中一个编入新集团军群；不拿唯一的集团军去掏空旧群
6. 战线
7. 防线和填线
8. 支援邻近战斗
9. 进攻线
10. 计划攻击

第 4 项未对照前不宣称能开第二个战区。不得把 `CSetTheatreCommand` 当创建。

## 架构约定

```text
HOI4 进程:  bridge.dll（version_gate / reader / 游戏线程发单）
                |
           共享内存 / 命名管道
                |
外部进程:  planner.exe（快照规范化 / 世界模型 / 分层规划 / 意图 / 回放）
工具:      launcher、monitor、replay_viewer、test_runner
```

`bridge.dll`：校验版本、按当前适配器定位已确认函数、在游戏更新点复制状态、发布快照、在游戏线程提交已验证订单并返回结果。不规划、不随机、不长期持有 HOI4 指针。

`planner.exe`：把快照变成自有实体、跑分层规划、生成意图和订单请求、记录决策。不访问 HOI4 地址，不在执行器确认前宣称已执行。

每个游戏构建一个适配器，封装特征码、偏移、调用约定和订单参数。planner 不依赖地址。

启动：检查 exe 哈希 → 加载 DLL → 验证版本和特征码 → 启动 planner → 建立 IPC → 只读，等待显式允许写入。规划器或 IPC 失效时停止发单。

每个决策周期：游戏线程复制状态 → 发布 Snapshot → planner 更新世界模型并生成 Intent → 转成 OrderRequest → 游戏线程二次校验后走原生命令 → 发布 OrderResult。

读档：清空对象映射和未提交订单，重建 `value + generation`，重新读战区和军队，只恢复仍然有效的意图。禁止复用旧指针。

P3 之后的分层：战略低频（敌人、战区优先级、攻守阶段）；战役中频（主攻/防守战区、军队角色、预备队）；战术只处理异常（突破、包围、组织度崩溃、补给中断）。默认师跟随所属军队计划，不持续微操。P0 未过不得开工这些层。

真人化是限制可见信息和操作频率（注意力预算、最小保持时间、命令冷却、每小时订单上限、置信度不足则不动），不是无约束随机。随机必须可记录种子。没有回放不得调参。

## 数据契约

快照用固定宽度字段和 `schema_version`。实体是 `value + generation`。不能把游戏内存指针传出进程。数组由快照所有者管理。

快照至少包含：国家；Theatre / Army Group / Army / Division（省份、组织度、当前订单摘要）；Front / 战斗；可见敌军估计；补给；存档和暂停状态。

planner 只输出意图（进攻、防守、增援、预备、整补、撤退、待机），不指定内存地址。bridge 把意图转成订单请求，字段包括：幂等键、实体 ID、订单类型、目标省份、期望快照序号、截止时间。

订单类型与上面「原生订单」清单一致：`Move`、`Cancel`、`SetTheatre`、`ArmyGroup`、`AssignArmy`、`NewFront`，以及后续的防线、支援、进攻线、撤退。

结果必须能区分：`accepted`、`rejected`、`executed`、`cancelled`、`expired`、`stale_object`、`invalid_target`、`conflict`、`unknown_error`。日志至少关联：游戏时间、快照序号、plan_id、实体 ID/generation、adapter 版本、错误码。

回放（M0，不是 P0）：能在不启动 HOI4 的情况下重跑 planner，比较意图和订单。事件包括快照、意图、下单、接受/拒绝/执行/取消、反馈、改计划。

## 阶段

P0 是生死门。结果表与验收文档一一对应。注入、读师、原生移动、抗覆盖任一项失败则放弃。

1. 注入、版本识别、安全退出：可重复加载且不叠 hook，停用后游戏能回到原生 AI。
2. 实时读一个已钉国家和一个师：ID、省份、组织度与游戏内一致。`GetPlayer` 另记；P0 用德国 tag 1。
3. 一次原生移动：接受、地图显示、实际执行、可取消。存档外观另属读档项。
4. 对抗 AI 覆盖：观察窗口内订单保持，或该国陆军行动者被抑制。禁止重下单硬抗。关的是微操、集体移动、支援军、区域防卫，拦集团军群/集团军发布口。不要整段跳过 `0x00EEA3E0`（停招募），不要跳过 `CTheatre` ctor（崩溃），不要整段让 `0x00EE67F0` 返回 0（停部署）。战区用原生命令接管。焊死计划组、关全局 `ai`、拦截工厂口，都不算通过。`CCountryAI[145]` 还发科研，不能整段跳过。
5. 读档重建：旧指针和旧 generation 全部失效，未完成订单清空，不会控错单位。

P0 过后只进 P1–P3 和为发单服务的世界模型。单师闭环属 P4，须先过 M0。战区原生命令取证可以在陆地移动成立后单独做，不属于 P6。

| 阶段 | 目标 | 计划验收 |
|---|---|---|
| P1 | 版本门控、快照、订单队列、失败停用 | 版本/指纹失败不装 hook 并停用；IPC 可断开恢复；planner 退出不冻游戏 |
| P2 | 自有实体、战区/战线关联、读档、快照录制 | 读档后旧 generation 失效；快照无游戏指针；对象 ID 连续稳定 |
| P3 | 离线三层规划、意图、状态机、回放 | planner 不依赖 HOI4；同快照同种子结果相同；订单有幂等键和过期 |
| M0 | 进入 P4 之前 | 快照连续稳定；结果能回 planner；失败能回退；回放可复现；观察窗口保住 |
| P4 | 单师闭环交给 planner | 移动/取消/撤退走原生命令；不足时能暂停；不无限重试 |
| P5 | 多师角色、预备队、订单预算 | 角色稳定；无互斥命令；不每个 tick 全量重写 |
| P6 | 防线、进攻线、战役反应 | 用已取证订单；不在这一阶段才找战区入口；每种订单仍单独取证 |
| P7 | 多战区、长时间、版本拒绝 | 读档可恢复或安全清除；任一失效能回退；旧 adapter 拒绝加载 |

## 风险

| 风险 | 对策 |
|---|---|
| 构建更新导致 ABI 变化 | 适配器 + 特征码，对不上就拒绝 |
| 找不到移动入口 | 先走玩家命令路径；确认不了则放弃项目 |
| 后续订单入口找不到 | 停在已证实集合 |
| 原版/BlackICE 覆盖订单 | P0 否决项：抑制行动者；做不到则放弃 |
| 用单师移动宣称战线控制 | 每种订单单独取证 |
| 把战区组建锁到 P6 | 原生命令取证与战役规划分开 |
| 读档后控错单位 | generation，读档全量重建 |
| 游戏线程宽扫 | 只碰已钉窗口 |

禁止项见 `AGENTS.md`。本文额外两条：不要把战区原生命令锁到 P6；不要整段跳过会停招募的函数来拦战区。
