# AI 指挥系统重构计划

截至 2026-09-13。

## 文档定位

本文件只写尚未实现的路线：架构、数据契约、阶段、风险和计划验收。长期不变量见 `AGENTS.md`。已实现状态和证据只见 `AI_IMPLEMENTED_FEATURES_ACCEPTANCE.md`。当前构建的逆向步骤和废路见 `AI_REVERSE_ENGINEERING.md`。

本文件不代表工程已经拥有 DLL、逆向符号、外部规划器、原生订单接口或逐师控制能力。P0 与 M0 的定义只在本文维护，验收文档只记录对它们的实测结果。

## 目标和成功定义

唯一目标：让控制器在单机里实时、自治、像玩家一样使用陆军。

```text
感知 -> 建模 -> 计划 -> 下令 -> 等待和观察 -> 反馈 -> 重新计划
```

控制器最终应能够：

- 观察国家、军队、师、战区、战线、战斗、补给和地形；
- 为军队和师分配进攻、防守、增援、预备队和整补角色；
- 通过游戏原生命令执行移动、防线、进攻线、支援和撤退；
- 根据战斗结果、损耗和补给变化调整计划；
- 在信息延迟、注意力限制和命令冷却下保持稳定行为；
- 保存、恢复和解释自己的决策过程。

交付物是本地启动器、`bridge.dll` 和 `planner.exe`。这是实现约束，不是另一个产品目标。

### 最小可交付

第一版只要求单机、单个国家、陆军、单个师的闭环稳定：

```text
实时读取位置和组织度
-> 发出一次原生移动命令
-> 游戏接受、地图显示、师实际执行
-> 观察窗口内订单保持，或该国陆军 AI 已被抑制
-> 读档后旧对象失效
-> 条件变化后可以取消或重新规划
```

这个闭环做不到，就放弃整个项目。

### 门槛关系

```text
P0 任一项失败 -> 放弃项目
P0 全部通过 -> 可以进入 P1 至 P3
M0 全部通过 -> 可以进入 P4
对应订单各自取证 -> 可以进入该订单所在的 P5 / P6
```

P0 证明项目活着。M0 证明单师闭环可以稳定交给规划器。单师移动不得作为防线、进攻线或战役控制的证据。

### 观察窗口

恢复运行后连续 12 个游戏小时，或该师到达目标省份，以先到者为准。窗口内原版或 BlackICE 不得改写该师订单，除非已经确认该国陆军 AI 被抑制。

## 设计原则

1. 唯一目标优先。有更直接、更能在游戏里验证的实现，直接用。
2. DLL 只接触游戏，不承载复杂 AI。
3. 规划器只处理自有快照，不保存游戏对象指针。
4. 版本差异只能存在于适配器中。
5. 规划器输出意图，执行器转换成原生订单。
6. 所有订单在游戏线程提交。
7. 每个订单可追踪、可取消、可过期、可重试。
8. 决策必须能录制、回放和离线测试。
9. 版本、对象或函数无法确认时停用写入。只读是运行时保险，不是成功形态。
10. 冷路径、从未被调用的函数或只满足取证形式的方案，换成能采到数、能下成单的做法。

## 总体架构

```text
+------------------- HOI4 进程 -------------------+
|  bridge.dll                                      |
|   version_gate                                   |
|   hook_manager                                   |
|   game_reader  ----> snapshot publisher          |
|   order_submitter <---- command consumer         |
+--------------------------------------------------+
                 | shared memory / named pipe
                 v
+-------------------- 外部进程 --------------------+
| planner.exe                                      |
|   snapshot_normalizer                            |
|   world_model                                    |
|   strategic_planner                              |
|   operational_planner                            |
|   tactical_manager                               |
|   human_behavior_model                           |
|   order_builder                                  |
|   replay_recorder                                |
+--------------------------------------------------+
                 |
                 v
+-------------------- 工具 ------------------------+
| launcher | monitor | replay_viewer | test_runner |
+--------------------------------------------------+
```

### 组件职责

`bridge.dll` 只负责校验版本、定位已确认函数、在游戏更新点复制状态、发布快照、在游戏线程提交已验证订单、返回结果。不负责战略评分、路径搜索、随机行为或长期计划。

`planner.exe` 负责把快照变成统一格式、维护世界模型、运行分层规划、施加信息与注意力限制、生成意图和订单请求、记录决策、离线回放。

`build adapter` 按 HOI4 构建封装特征码、偏移、函数签名、调用约定、订单参数转换和能力列表。规划器不能依赖任何具体地址或偏移。

## 数据契约

快照在 DLL 和规划器之间使用固定宽度字段。数组由快照所有者管理，不能传递指向游戏内存的裸指针。

```cpp
struct EntityId {
    int32_t value;
    uint32_t generation;
};

struct DivisionState {
    EntityId id;
    EntityId army;
    int32_t owner_country;
    int32_t province;
    float organization;
    float strength;
    float equipment;
    float supply;
    uint8_t in_combat;
    uint8_t visible_to_controller;
};

struct SnapshotHeader {
    uint32_t schema_version;
    uint32_t game_build;
    uint64_t sequence;
    uint64_t game_time;
};
```

完整快照还应包含：国家和战争关系；Army、Army Group；Division 所在省份和当前订单摘要；Front、Frontline、Battle；省份连接、地形、天气和河流；补给、燃油和可见敌军估计；存档、读档和暂停状态。

规划器只生成意图，不指定内存地址：

```cpp
enum class IntentType : uint8_t {
    Defend,
    Attack,
    Reinforce,
    Reserve,
    Recover,
    Retreat,
    Hold
};

struct Intent {
    uint64_t plan_id;
    EntityId owner;
    IntentType type;
    int32_t objective_province;
    float priority;
    float confidence;
    uint64_t expires_at;
};
```

订单适配器把意图转成请求：

```cpp
enum class OrderType : uint8_t {
    Move,
    Frontline,
    OffensiveLine,
    Support,
    Retreat,
    Cancel
};

struct OrderRequest {
    uint64_t plan_id;
    uint64_t idempotency_key;
    EntityId division;
    OrderType type;
    int32_t target_province;
    uint64_t expected_snapshot;
    uint64_t deadline;
};
```

执行结果必须能区分：`accepted`、`rejected`、`executed`、`cancelled`、`expired`、`stale_object`、`invalid_target`、`conflict`、`unknown_error`。

快照、意图、订单和结果至少关联：`game_time`、`snapshot sequence`、`plan_id`、实体 ID/generation、adapter/build version、decision seed、error code。

## 运行时生命周期

启动：

```text
启动器检查 exe 哈希
-> 创建 HOI4 进程
-> 加载 bridge.dll
-> 验证版本和特征码
-> 启动 planner.exe
-> 建立 IPC
-> 进入只读，等待显式允许写入
```

每个决策周期：

```text
游戏线程复制原始状态
-> bridge 发布 Snapshot
-> planner 更新 WorldModel
-> planner 生成 Intent
-> order_builder 生成 OrderRequest
-> bridge 在游戏线程二次校验
-> 调用原生订单函数
-> 发布 OrderResult
-> planner 写入事件日志
```

规划器或 IPC 失效时，bridge 必须停止发单。

读档后不得复用旧指针：

```text
检测读档
-> 清空对象映射和未提交订单
-> 重建实体 ID/generation
-> 重新读取战区和军队
-> 验证旧计划目标
-> 只恢复仍然有效的意图
```

## 分层决策模型

战略规划器低频决定主要敌人、战区优先级、风险容忍度、攻守或恢复阶段、资源和预备队总量。

战役规划器中频决定主攻战区、防守或整补战区、军队角色、进攻准备条件、预备队和增援路线、战役暂停与失败条件。

战术管理器高频只处理异常：局部突破或包围、组织度过低、损耗异常、敌方援军、补给中断、撤退和临时填线。默认情况下师遵循所属军队计划，不持续微操。

这些层是 P3 之后的设计，P0 未通过前不得开工。

## 真人行为模型

真人化是限制信息和操作能力，不是加无约束随机数。

信息限制：只使用当前可见对象和延迟快照；敌军数量用估计区间；不读隐藏战斗结果；不用未来几小时的预测当既成事实。

行为限制：军队有注意力预算；订单有最小保持时间；同一师有命令冷却；每小时有最大订单数；计划有惯性；置信度不足时保持现状；攻击前要准备；战斗失败后优先撤退或整补；不同军队有不同风险偏好和可记录的错误概率。

随机决策使用可记录种子。本模型在 M0 之后调参，没有回放不得调参。

## 原生订单路线

按风险递增取证：

1. 单师移动
2. 单师取消和撤退
3. 单师加入或移出军队
4. 防线和填线
5. 支援邻近战斗
6. 进攻线
7. 计划攻击
8. 多军队重部署

优先找玩家操作经过的命令路径。每种订单必须单独通过：

```text
创建 -> 游戏接受 -> 地图显示 -> 实际执行 -> 可取消 -> 存档后状态正常
```

P0 的移动入口无法确认，放弃项目。P0 之后某一种后续订单入口无法确认，则停止在该订单之前的已证实集合，不得写字段冒充，也不得宣称更高阶段已经达成。

## 回放和事件溯源

每个周期写入不可变事件：`SnapshotCaptured`、`WorldModelUpdated`、`IntentCreated`、`OrderRequested`、`OrderAccepted`、`OrderRejected`、`OrderExecuted`、`OrderCancelled`、`FeedbackObserved`、`PlanRevised`。

回放工具必须能在不启动 HOI4 的情况下重跑规划器，并比较实际订单与回放订单、计划变化、订单抖动、撤退时机、补给不足时的行为。回放是 M0 的一部分，不是 P0 的一部分。

## 开发阶段

### P0：可行性探针

P0 是项目生死门。按顺序验收，任一项失败都放弃整个项目。验收记录行与 `AI_IMPLEMENTED_FEATURES_ACCEPTANCE.md` 的结果表一一对应。

1. 注入、版本识别、安全退出
   - 记录行：DLL 加载、版本识别、停用和回退
   - 通过：游戏不崩溃，能识别当前构建，可重复加载且不重复安装 hook，停用后游戏可回到原生 AI
2. 实时读取当前国家和一个师
   - 记录行：国家读取、师对象读取
   - 通过：师 ID、省份、组织度与游戏内一致，暂停时也稳定。读档解析不能替代实时读取
3. 一次原生移动
   - 记录行：移动订单创建、移动订单执行、订单取消
   - 通过：游戏接受订单，地图显示移动，师实际执行，可取消，存档后状态正常
4. 对抗原版 / BlackICE AI 覆盖
   - 记录行：对抗 AI 覆盖或抑制陆军 AI
   - 通过：观察窗口内订单保持，或已确认该国陆军 AI 被抑制且不会改写该师。禁止重下单硬抗
   - 通过标准是主要国家（先德国，tag 1–7）不再新建战区/集团军并发移动单；不得靠焊死计划组、关掉全局 `ai`、或拦截我们自己以后也要用的组建口来凑数
5. 读档重建
   - 记录行：读档重建
   - 通过：旧指针与旧 generation 全部失效，未完成订单被清空，不会控错单位

P0 通过后只允许进入 P1 至 P3，以及为发单服务的世界模型。不允许进入 P4 或战区级控制。

### P1：薄桥接和版本适配器

目标：版本门控、特征码扫描、快照发布、订单队列、自动禁用和日志。

计划验收：版本不匹配时不安装 hook；特征码失败时自动停用；bridge 不阻塞游戏线程；快照和订单 IPC 能建立、断开和恢复；planner 退出不会冻结游戏。

### P2：外部世界模型

目标：统一实体结构、对象生命周期、战区战线和战斗关联、存档读档重建、快照录制。

计划验收：国家、军队、师、战线和战斗能映射到自有实体；读档后旧 generation 全部失效；快照不含游戏内存裸指针；连续快照中对象 ID 稳定；录制文件可以独立读取。

### P3：离线规划器

目标：战略、战役、战术三层，意图和订单请求，计划状态机，真人行为参数，无游戏运行的回放测试。

计划验收：planner 不依赖 HOI4 进程；相同快照和随机种子产生相同结果；意图和订单请求可解释；计划具有准备、执行、暂停、恢复和失效条件；订单请求有幂等键、过期时间和优先级。

### M0：进入 P4 之前

P0 仍然成立，并且：

```text
只读快照连续稳定
+ 订单执行结果可被规划器确认
+ 失败和停用能够回退
+ 回放结果可复现
+ 观察窗口内订单不被覆盖，或该国陆军 AI 已被抑制
```

M0 未通过前不得进入 P4。

### P4：单师控制闭环

目标：移动、停止、撤退、补给不足暂停、订单冷却、反馈后重新规划。

计划验收：单师移动、取消和撤退使用原生订单；订单结果能回到 planner；补给或组织度不足时能暂停；同一订单不会被重复提交；失败不会无限重试。

### P5：军队级控制

目标：军队角色、预备队、填线和增援、有限订单预算、多师冲突解决。

计划验收：多师角色分配稳定；预备队和增援有明确条件；订单预算生效；师之间没有互斥命令；不会每个 tick 重写全部命令。每种新增订单仍需单独取证。

### P6：战线和战役控制

目标：防线、进攻线、计划攻击、局部突破和包围反应、战役暂停与失败。

计划验收：防线、进攻线和计划攻击分别通过独立能力测试；局部突破、包围和补给中断能触发重新规划；战役暂停、恢复和失败原因可追踪；原生订单和控制器计划不会互相覆盖。

### P7：长期运行

目标：多战区、存档读档、数十小时稳定性、版本拒绝、DLL 停用后恢复原生 AI、录制结果可重放。

计划验收：多战区运行数十小时无崩溃；读档后计划可恢复或安全清除；planner、IPC、hook 任一失效都能回退；日志和回放足以复现关键决策；HOI4 更新后旧 adapter 拒绝加载。

## 主要风险

| 风险 | 影响 | 对策 |
|---|---|---|
| HOI4 更新导致 ABI 变化 | DLL 崩溃或错误写入 | 构建适配器、特征码验证、版本拒绝 |
| 找不到稳定的移动订单入口 | 唯一目标无法成立 | 先验证 UI/命令路径；仍无法确认则放弃项目 |
| 后续订单入口找不到 | 无法进入对应阶段 | 停在已证实的订单集合，不得写字段冒充 |
| BlackICE 或原生 AI 覆盖订单 | 计划无法保持 | P0 否决项：保持或抑制陆军 AI；做不到则放弃 |
| 用单师移动推断战线控制 | 过早进入 P6 | 每种订单单独取证 |
| 对象在读档后失效 | 误控错误单位 | generation ID，读档后全量重建 |
| 高频订单导致性能下降 | 卡顿和行为抖动 | 订单预算、最小保持时间、事件触发 |
| 规划器失控 | 游戏冻结或连续发单 | watchdog、超时、队列上限和紧急停用 |
| 游戏线程宽扫未知内存 | 卡死 | 只碰已钉扎的小窗口；失败就换入口，不扫 |

## 明确不采用

- 把完整 AI 写进 DLL
- 直接修改师的位置、战斗胜负、组织度或资源
- 通过内存写入伪造原生订单
- 使用固定地址而没有版本验证
- 让后台线程直接操作游戏对象
- 以每个 tick 全量重下订单作为控制循环
- 没有回放和日志就进行真人化参数调优
- 用提高重下单频率对抗 AI 覆盖
- 把 DLL 放进普通 mod 目录并按创意工坊产品验收
- 用一种订单的成功推断其他订单可用
- 把目标改成脚本偏置、只读参谋或其他替代产品
- 在发单或抗覆盖失败后继续做分析器并宣称项目仍在推进
- 焊死计划组、关掉全局 `ai`、或把以后自己要用的战区/集团军创建口当成产品闸
