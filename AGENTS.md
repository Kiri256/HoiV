# AI 指挥系统工程规则

## 唯一目标

在单机里实时、自治地使用陆军作战：指挥已有部队，并控制战区、集团军群、集团军、战线。做不到就放弃整个项目。

不改师模板编制（几个团、几个支援连）。不干预运营（招募、科研、建造、生产、国策、决议）。不得改成脚本偏置、只读参谋、创意工坊 AI 或其他替代产品。交付物是本地启动器、`bridge.dll` 和 `planner.exe`。

方法从属于这个目标。有更直接、更能在游戏里验证的做法，直接用。不要为迁就某一种 hook、取证仪式或文档格式停在不能工作的路径上。

未实现路线见 `AI_REALISM_OPTIMIZATION_PLAN.md`。已实现状态和证据只以 `AI_IMPLEMENTED_FEATURES_ACCEPTANCE.md` 为准。没有游戏内证据，不得标为已实现。当前构建的逆向步骤和废路见 `AI_REVERSE_ENGINEERING.md`，那不是验收。

## 方法

- 先选能跑通目标的路径，再固化到当前构建的适配器。旧地址、猜测写入、失败后瞎猜备用写入口仍然禁止。
- 读取：任何能在游戏线程上得到正确实时状态的方式都可以。冷函数、从不被调用的 getter 不是合格采样点，发现了就换。
- 一种手段失败，立刻换更好的手段。不要把「已经装上 hook」当成进展。
- 写入只走游戏原生订单。禁止改师位置、战斗结果、组织度、资源、路径或胜负来冒充操作。
- 实时读取到单师原生移动的闭环做不到，就放弃项目。该闭环未通过前，不扩展到未证实的订单类型或海空军。一种订单成功不能推断另一种可用。不发改师模板或招募的命令。战区、集团军群、集团军、战线用原生命令，未对照前不发。战区和集团军群都必须至少挂一个集团军：用原生命令把集团军编进去，不建空对象。

## 防止走形的边界

这些不是独立教条，只是防止产品变成另一件事：

- `bridge.dll`：版本检查、状态采集、游戏线程提交订单。不规划、不随机、不长期持久化，不把 HOI4 指针留给下一帧或发给 planner。
- `planner`：只处理自有快照和世界模型。不访问 HOI4 地址，不在执行器确认前宣称订单已执行，不用全知状态。
- 每个游戏构建一个适配器。planner 不依赖地址、偏移或调用约定。
- 订单要幂等，并做对象、目标、冲突和过期检查；只在游戏线程提交。游戏接受一次订单不等于控制成立：观察窗口内不得被原版或 BlackICE 覆盖，或该国陆军 AI 必须已被抑制。禁止靠重下单硬抗。
- 抑制陆军 AI 时：关它调动已有部队的行动者，并拦它自行组建战区、集团军群、集团军的发布口。这些对象以后用原生命令组建，禁止焊死对象本身。不改师模板，不接管招募、科研、生产。焊死计划机构或全局关掉全部 AI，都是走形。
- 默认 `enabled=0`、`read_only=1`、`max_orders_per_hour=0`。版本、IPC、对象或异常失败时停用发单。只读是保险，不是成功形态。
- 跨模块用固定宽度字段和 `schema_version`。实体用 `value + generation`。异常不穿过游戏函数边界。
- 第一版：一个明确的 HOI4 1.19.x 构建、Windows x64、BlackICE 12.1.x、单机、一个国家的陆军。游戏更新后重新确认当前构建。
- 禁止在游戏线程上对未知窗口做 `VirtualQuery` 宽扫。

## 文件职责

- `AGENTS.md`：唯一目标和防止走形的边界。
- `AI_REALISM_OPTIMIZATION_PLAN.md`：当前下一步、门槛、架构约定和未实现阶段。
- `AI_IMPLEMENTED_FEATURES_ACCEPTANCE.md`：已实现状态、环境和证据。
- `AI_REVERSE_ENGINEERING.md`：当前钉扎构建的逆向步骤、证据和废路。不是验收。

修改后执行 `git diff --check`。检查是否仍指向唯一目标，以及 ABI、线程边界、订单幂等和版本门控有没有被顺手破坏。

## 命令执行

所有 shell 命令用干净、无 `&&` 连贯的长路径写法。PowerShell 引号或 `&&` 解析失败时立即停掉，换干净写法重跑。不得把失败或卡住的命令写成「已执行」。
