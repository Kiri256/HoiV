---
description: 唯一目标优先；实现时直接采用更好的方法
alwaysApply: true
---

# 唯一目标

在单机里实时、自治、像玩家一样使用陆军。做不到就放弃项目。不要改成脚本、只读参谋或创意工坊 mod。

方法从属于目标。有更直接、更能在游戏里验证的实现，直接用。冷 hook、从未被调用的函数、只满足「已取证」但采不到数的方案，换掉。

读取：任何能在游戏线程得到正确实时状态的方式都可以（单例、IAT、消息泵、虚表确认扫描）。
写入：只走原生订单，不改位置/组织度/战斗/资源来造假。
闭环未通前不扩展订单类型或海空军。没有游戏内证据不得标已实现。
