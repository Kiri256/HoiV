# 已实现功能与验收记录

截至 2026-09-20。

## 文档定位

本文只记录已经写入仓库、能够被构建或在游戏内复现的功能及其证据。计划中的架构、阶段和门槛见 `AI_REALISM_OPTIMIZATION_PLAN.md`。长期规则见 `AGENTS.md`。逆向步骤和废路见 `AI_REVERSE_ENGINEERING.md`。

没有游戏内证据，不得标为已实现。试错过程不在这里展开。

## 环境

```text
HOI4：1.19.1.0（launcher-settings.json rawVersion；exe VERSIONINFO 1.0.0.0 不能单独门控）
SHA-256：16022851eaa571728aa3a0b5fd5f990a56da57e2249a4c1b9cfb30336f59b120
PE 时间戳：1781617671
BlackICE：12.1.0
OS：Windows 10 10.0.26100 x64
adapter：hoi4_1_19_blackice_12_1
热路径：德国 tag 1。瑞士槽 29 / 圣加仑 11623 / 句柄 51/2476 只作历史对照
schema_version：10
默认配置：enabled=0、read_only=1、max_orders_per_hour=0
```

## 当前状态

| 能力 | 状态 | 证据 |
|---|---|---|
| bridge.dll / 启动器 | 已加载 | 2026-09-13 `--status`：`version_ok=1 hooks=1 writes=0`，哈希与 `1.19.1.0` 一致。`--disable` 后 `hooks=0 disabled=1` |
| 版本门控 | 通过 | 钉扎 SHA-256 与 PE 时间戳匹配 |
| 只读布局 | 已钉 | 国家数组 `+0x310/+0x31C`，师列表 `+0x290`，位置 `+0x1F0`，省份 `+0xA4`，句柄 `+0x18`，组织度 `+0x428`，HP `+0x420` |
| 任意当前玩家识别 | 未通过 | `GetPlayer` 仍为 0。德国 tag 1 是钉槽，不是通用识别 |
| 师对象读取 | 通过 | 瑞士圣加仑 `51/2476` 省份 11623、HP 166.9。德国斯德丁 `51/1` HP 249.5，6282→3312，句柄不变 |
| 状态快照 | 部分实现 | `SharedBlock` schema 10，不含游戏指针。热路径钉德国 tag 1 |
| planner.exe | 占位 | 只连接会话并退出，不规划 |
| 世界模型 / 规划 | 未实现 | 无运行时代码 |
| 原生移动 | 部分实现 | 瑞士 `51/2476` 走到 11604，途中取消停在 11601。德国 `51/1` 抑制仍在时 `--move 3312`，`result=4`，6282→3312。德国 `--cancel` 未做 |
| 德国陆军行动者抑制 | 部分通过 | 本国微操、集体移动、支援军已停；科研在只拦 `CMassMoveCommand[9]` 后恢复。不能宣称已经控制整支陆军 |
| 战区 / 集团军群 / 集团军 | 部分实现 | 2026-09-20 `--army`：`attempts=1 accepted=1 result=4`。操作者确认编制栏同时出现「德国第1战区」、集团军群和集团军（第一条集团军带出不能空的父级）。手建第二个集团军后，「新战区」能把其中一个集团军调到新战区，源战区留下另一个。随后 `--ag` `result=1` Gated，未对照。额外战区走 `CAssignToTheaterGroupCommand`，未发。不建空战区/空集团军群 |
| 防线、进攻线、支援、撤退 | 未实现 | 无订单适配器 |
| 反馈闭环 / 读档 / 回放 | 未实现 | 无 |

## P0 结果

门槛定义见计划。注入、读师、原生移动、抗覆盖做不到则放弃；任意玩家识别和读档未通过则不得进入后续阶段。

| 项目 | 结果 | 证据 |
|---|---|---|
| DLL 加载 | 通过 | 2026-09-13 `schema=1 version_ok=1 hooks=1 writes=0` |
| 版本识别 | 通过 | `product_version=1.19.1.0` |
| 国家读取 | 未通过 | 能读国家数组和钉槽；`GetPlayer` 为 0，不能当任意玩家识别 |
| 师对象读取 | 通过 | 瑞士 `51/2476`；德国 `51/1` |
| 移动订单创建 | 通过 | 瑞士 `--move` 11604 `result=4`。德国 `--move 3312` `attempts=1 accepted=1 result=4` |
| 移动订单执行 | 通过 | 瑞士到达 11604。德国 6282→3312，HP 249.5，`id/gen` 不变。操作者确认移动 |
| 订单取消 | 通过 | 瑞士 `51/2476` 在 11601 `--cancel` 得 `result=8`，师停下。德国未测 |
| 对抗 AI 覆盖 | 部分通过 | 德国 1936：微操与支援军已停；`ag skip=73952` 后地图无集团军群/集团军。对 `51/1` 发移动到达时 `move skip` 仍涨。不拦 Idler[4]、不关工厂 |
| 读档重建 | 未通过 | 未实现 |
| 停用和回退 | 通过 | `--disable` 卸载 hook。关掉游戏、不注入重开，原版 AI 正常。同进程 `--disable` 后是否立刻恢复走动本局未测 |

观察窗口：德国 1936 本国师停住、支援军停住、采样师原生移动到达。手控瑞士到达 11604 不能当窗口证据。

### 仍未通过的说明

**国家读取：** `GetPlayer` 在瑞士战役不被调用。不能用 `+0x145C`、Idler `+0x4F0` 或「第一个有军队的国家」定位玩家。热路径钉德国 tag 1，不是通用识别。

**对抗 AI：** 2026-09-13 `--no-land-ai` 各闸失败，见逆向废路，不得再用。2026-09-20 德国只关行动者后科研曾被整段 `[145]` 误伤，改为只拦 `CMassMoveCommand[9]` 后恢复。整段跳过 `0x00EEA3E0` 会停招募，已废。跳过 `CTheatre` ctor 会内战前崩溃，已废。整段让 `0x00EE67F0` 返回 0 会停部署，已废。

### 离线证据

`build/p0_tests.exe`：P0.1 / P0.2 通过。覆盖版本拒绝、hook 不叠装、卸载后宿主存活、`write_enabled` 保持 0、当前适配器指纹。这不是游戏内证据。

### 加载证据

2026-09-13 重复 `--load` 时 `installs=2` 且 `hooks=1`，没有把 hook 叠成两份。写入默认关闭。

## 不得标记为已实现

- 任意当前国家/玩家识别
- 德国 `--cancel`，以及除已取证移动和 `--army` 外的其他原生订单
- 单独发 `CArmyGroupCommand` / `CAssignToTheaterGroupCommand` 再出一个集团军群或新战区
- 用原生命令组建战线
- 已经控制整支德国陆军
- 同进程 `--disable` 后立刻恢复走动
- 读档重建、外部规划、存档恢复、多人同步、真人化、长时间稳定性

## 静态检查

每次变更执行 `git diff --check`，并核对 ABI schema、hook 安装和卸载、游戏线程与规划线程边界、订单幂等、版本门控，以及本文状态与代码是否一致。
